# Capability Plugins

This tree is reserved for capability plugins that are not runtime extensions.

Use it for host-facing plugin families with their own packaging and execution model.

Source artifacts use `@<organization>/<artifact>/`; official node capabilities are
kept under `@taichuy/nodes/`.
