# Session Retry Distribution

Official `provider_distribution_rule/v1` RuntimeExtension. It stores conversation affinity only in its Host-managed `affinity` collection and returns a typed selection decision; retry count and Provider execution remain owned by 1flowbase Orchestration.
