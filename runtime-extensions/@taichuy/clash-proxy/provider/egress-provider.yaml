provider_code: clash-proxy
display_name: Clash / Mihomo Proxy
form_schema:
  schema_version: 1flowbase.plugin.form/v1
  fields:
    - key: subscription_url
      label: Subscription URL
      type: string
      control: url
      required: true
      send_mode: secret
      description: HTTPS URL for the Clash or Mihomo subscription to synchronize.
